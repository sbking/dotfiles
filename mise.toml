[tasks.validate]
description = "Run the local dotfiles validation suite"
run = "scripts/validate.sh"

[tasks."validate:quick"]
description = "Run syntax, whitespace, and chezmoi checks without optional runtime smoke tests"
run = "scripts/validate.sh --quick"
